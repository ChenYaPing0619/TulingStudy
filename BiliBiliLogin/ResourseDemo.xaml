<Window x:Class="BiliBiliLogin.ResourseDemo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BiliBiliLogin"
        mc:Ignorable="d"
        Title="ResourseDemo" Height="450" Width="800">
    <Window.Resources>
        <SolidColorBrush x:Key="SolidColorBrushOne" Color="Red"/>
        <SolidColorBrush x:Key="SolidColorBrushTwo" Color="Blue"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
       
        <Border  Grid.Row="0" Grid.Column="0" Background="{StaticResource SolidColorBrushOne}"/>
        <Border  Grid.Row="0" Grid.Column="1" Background="{StaticResource SolidColorBrushTwo}"/>
        <Border  Grid.Row="1" Grid.Column="0" Background="{DynamicResource SolidColorBrushThree}"/>
        <Border  Grid.Row="1" Grid.Column="1" Background="{DynamicResource SolidColorBrushFour}"/>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <Button Width="50"  Content="Change"  Foreground="Black" Click="Button_Click"/>
        </StackPanel>
    </Grid>
   
</Window>
