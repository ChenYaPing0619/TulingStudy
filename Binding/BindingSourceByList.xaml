<Window x:Class="BindingDemo.BindingSourceByList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BindingDemo"
        mc:Ignorable="d"
        Title="BindingSourceByList" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="请选择学生:"/>
            <ListBox Name="StudentNameList">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Margin="5"/>
                            <TextBlock Text="{Binding Score}" Margin="5"/>
                            <Button Content="Edit" Background="LightGoldenrodYellow" Margin="5"/>
                        </StackPanel>
                       
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock Text="成绩:"/>
            <TextBox Text="{Binding ElementName=StudentNameList, Path=SelectedItem.Score}"/>

        </StackPanel>
    </Grid>
</Window>
